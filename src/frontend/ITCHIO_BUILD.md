# Building for itch.io

This guide explains how to build and upload your game to itch.io as an HTML5 project.

## Build Output Folder

By default, the production build outputs to a folder named **`dist`**.

### Changing the Build Folder Name

If you want to use a different folder name (e.g., `build`, `out`, or `game`), set the `BUILD_OUT_DIR` environment variable before building:

**On macOS/Linux:**
